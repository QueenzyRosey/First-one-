# A simple number guessing game
# Could you make something cooler?
import random

# Make n a  random between 1 and 300
n = random.randint(1, 300)

# Ask user to guess the number 
guess = int(input("Enter an integer from 1 to 300: "))

while n != "guess":
    print
    if guess < n:
        print("guess is low")
        guess = int(input("Enter an integer from 1 to 300: "))
    elif guess > n:
        print("guess is high")
        guess = int(input("Enter an integer from 1 to 300 : "))
    else:
        print("you guessed it")
        break
    print 
